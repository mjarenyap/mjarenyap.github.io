/* Javacript handler for projects */

$(document).ready(function(){
    var projectWrappers = $('div.project-wrapper');
    
    $(projectWrappers[projectWrappers.length - 1]).css('border-bottom', 'none');
});